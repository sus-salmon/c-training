/*
 * Задача: 01-01-hello-world
 * Цели изучения:
 *  - Понять базовую структуру программы на C (функция main, заголовочные файлы)
 *  - Изучить функцию printf() для вывода текста на экран
 *  - Познакомиться с процессом компиляции и запуска программы на C
 *  - Освоить основной цикл разработки: написать → собрать → запустить → протестировать
 *  - Понять роль возвращаемого значения функции main() (код завершения программы)
 * Заметки:
 *  - Сборка: make build
 *  - Запуск:   ./build/01-01-hello-world
 *  - Тест:  make test
 *
 * TODO: Реализуйте функционал программы, чтобы все тесты прошли!
 * Тесты уже написаны - практикуйте Test-Driven Development (TDD)
 * запуская 'make test' часто при разработке программы.
 */

#include <stdio.h>   // Подключаем библиотеку для работы с вводом/выводом (printf, scanf)
#include <stdlib.h>  // Подключаем стандартную библиотеку (EXIT_SUCCESS, EXIT_FAILURE)

/*
 * Функция main() - это точка входа в любую программу на C.
 * Именно с неё начинается выполнение вашего кода.
 *
 * int - означает, что функция возвращает целое число (код завершения)
 * void - означает, что функция не принимает параметров
 *
 * Код завершения:
 * - 0 (или EXIT_SUCCESS) = программа завершилась успешно
 * - любое другое число = произошла ошибка
 */
int main(void) {
  // TODO: Выведите на экран сообщение "Hello, World!"
  //
  // ПОДСКАЗКИ ПО РЕАЛИЗАЦИИ:
  // 1. Используйте функцию printf() для вывода текста
  // 2. Не забудьте добавить символ новой строки \n в конце
  // 3. Проверьте точное соответствие вывода в тестах!
  //
  // ПРИМЕР ИСПОЛЬЗОВАНИЯ printf():
  // printf("Ваш текст здесь\n");
  //
  // ВНИМАНИЕ: Обратите внимание на формат вывода в тестах!
  // Текст должен точно соответствовать ожидаемому результату.

  printf("Hello, World!\n");

  return EXIT_SUCCESS;  // Возвращаем 0, сообщая операционной системе об успешном завершении
}
